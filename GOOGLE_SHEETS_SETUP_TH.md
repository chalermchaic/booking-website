# 📝 คู่มือการตั้งค่า Google Sheets สำหรับระบบจองคิว

ทำตามขั้นตอนด้านล่างเพื่อเชื่อมต่อเว็บไซต์จองคิวของคุณกับ Google Sheets

## ขั้นตอนที่ 1: สร้าง Google Sheet
1. ไปที่ [Google Sheets](https://sheets.google.com)
2. สร้าง Spreadsheet ใหม่ (Blank spreadsheet)
3. ตั้งชื่อไฟล์ตามต้องการ เช่น "Booking Data"
4. **สำคัญ:** เปลี่ยนชื่อ Sheet ด้านล่าง (Tab) จาก "Sheet1" เป็น **"Bookings"** (ตัวพิมพ์ใหญ่ B)

## ขั้นตอนที่ 2: เปิด Apps Script
1. ใน Google Sheet ไปที่เมนู **Extensions (ส่วนขยาย)** > **Apps Script**
2. จะมีหน้าต่างใหม่เปิดขึ้นมา
3. ลบโค้ดที่มีอยู่ทั้งหมดในไฟล์ `Code.gs` ออก

## ขั้นตอนที่ 3: ใส่โค้ด
1. เปิดไฟล์ `google-apps-script.js` ที่อยู่ในโฟลเดอร์โปรเจกต์ของคุณ
2. Copy โค้ดทั้งหมดจากไฟล์นั้น
3. Paste ลงในหน้าต่าง Apps Script แทนที่ของเดิม
4. กดปุ่ม Save (รูปแผ่นดิสก์) หรือ `Ctrl + S`

## ขั้นตอนที่ 4: Deploy (นำไปใช้จริง)
1. กดปุ่ม **Deploy** (สีน้ำเงินมุมขวาบน) > **New deployment**
2. กดที่รูปเฟือง (Select type) > เลือก **Web app**
3. ตั้งค่าดังนี้:
   - **Description:** Booking API (หรืออะไรก็ได้)
   - **Execute as:** Me (อีเมลของคุณ)
   - **Who has access:** **Anyone** (สำคัญมาก! ต้องเลือกอันนี้เพื่อให้เว็บไซต์ส่งข้อมูลได้โดยไม่ต้องล็อกอิน)
4. กด **Deploy**
5. (ครั้งแรก) Google จะขอสิทธิ์เข้าถึง (Authorize access)
   - กด **Authorize access**
   - เลือกบัญชี Google ของคุณ
   - ถ้าเจอหน้าจอ "Google hasn't verified this app" ให้กด **Advanced** > **Go to ... (unsafe)** (เพราะเป็นสคริปต์ที่คุณเขียนเอง ปลอดภัยแน่นอน)
   - กด **Allow**

## ขั้นตอนที่ 5: นำ URL มาใส่ในเว็บไซต์
1. หลังจาก Deploy สำเร็จ คุณจะได้ **Web App URL** (จะยาวๆ หน่อย ขึ้นต้นด้วย `https://script.google.com/...`)
2. Copy URL นั้นเก็บไว้
3. เปิดไฟล์ `booking-website-advanced.html` และ `booking-website.html` ในเครื่องของคุณ
4. ค้นหาตัวแปร `let googleSheetURL = '';`
5. นำ URL ที่ได้มาใส่ในเครื่องหมายคำพูด
   - ตัวอย่าง: `let googleSheetURL = 'https://script.google.com/macros/s/AKfycbx.../exec';`
6. บันทึกไฟล์ HTML

## เสร็จสิ้น! 🎉
ลองทดสอบจองคิวในหน้าเว็บ ข้อมูลควรจะเด้งเข้าไปใน Google Sheet ทันที
