# 📖 คู่มือฉบับสมบูรณ์: ตั้งค่าเว็บจองคิวให้ใช้งานจริง

---

## ⭐ STEP 1: Upload เว็บไปยัง Netlify (ใช้เวลา 5 นาที)

### 1.1 สมัครสมาชิก Netlify
1. ไปที่ **https://netlify.com**
2. กดปุ่ม **"Sign up"**
3. เลือก **"GitHub" หรือ "Google"** (ง่ายกว่า)
4. ตามขั้นตอนจนสำเร็จ

### 1.2 Upload ไฟล์
1. ไปที่ **https://app.netlify.com/drop**
2. **Drag and drop** ไฟล์ `booking-website-advanced.html` ลงในช่องขนาดใหญ่
3. รอสักครู่
4. จะได้ URL เช่น: `https://tiny-number.netlify.app`

### 1.3 แบ่งปัน URL ให้ลูกค้า
✅ เสร็จแล้ว! ตอนนี้ลูกค้าสามารถเข้าเว็บจองคิวของคุณได้แล้ว

---

## ⭐ STEP 2: ตั้ง Google Sheets เพื่อเก็บข้อมูล (ใช้เวลา 10 นาที)

### 2.1 สร้าง Google Sheet
1. ไปที่ **https://sheets.google.com**
2. เข้าสู่ระบบด้วย Google Account
3. กดปุ่ม **"+ Untitled spreadsheet"**
4. ตั้งชื่อว่า **"Bookings Database"** (ตัวอักษรห้ามเว้นวรรค)

### 2.2 สร้างคอลัมน์สำหรับเก็บข้อมูล
ใน Sheet ที่สร้างใหม่ ให้กรอกหัวตาราง:

| เซลล์ | ข้อมูล |
|-----|--------|
| A1 | Timestamp |
| B1 | ชื่อ |
| C1 | เบอร์โทร |
| D1 | อีเมล |
| E1 | บริการ |
| F1 | วัน |
| G1 | เวลา |
| H1 | หมายเหตุ |

✅ คลิกที่เซลล์ A1 แล้วพิมพ์ "Timestamp" แล้วกด Enter ทำเช่นนี้สำหรับทั้งหมด

### 2.3 หา Sheet ID
1. ดูที่ URL ของ Sheet เช่น:
   ```
   https://docs.google.com/spreadsheets/d/1ABC23XYZ4567def/edit
                              ↑ ตรงนี้คือ Sheet ID
   ```
2. **Copy** ส่วน ID (ตัวอักษรตรงกลาง) และเก็บไว้

---

## ⭐ STEP 3: สร้าง Google Apps Script (ใช้เวลา 15 นาที)

### 3.1 เปิด Google Apps Script
1. ไปที่ **https://script.google.com**
2. เข้าสู่ระบบด้วย Google Account เดียวกัน
3. กดปุ่ม **"New project"**

### 3.2 Copy Code ลงไป
ลบ Code ที่มีอยู่ แล้ว **Copy-Paste** Code นี้:

```javascript
function doPost(e) {
  try {
    // ⚠️ เปลี่ยน 'YOUR_SHEET_ID' เป็น Sheet ID ที่หา
    var sheet = SpreadsheetApp.openById('YOUR_SHEET_ID').getSheetByName('Sheet1');
    var data = JSON.parse(e.postData.contents);
    
    // บันทึกข้อมูลลงใน Sheet
    sheet.appendRow([
      new Date(),
      data.name,
      data.phone,
      data.email,
      data.serviceName,
      data.date,
      data.time,
      data.notes
    ]);
    
    // ส่ง Email ให้ลูกค้า
    sendConfirmationEmail(data.email, data);
    
    return ContentService.createTextOutput(JSON.stringify({status: "success"}))
      .setMimeType(ContentService.MimeType.JSON);
  } catch(error) {
    return ContentService.createTextOutput(JSON.stringify({status: "error", message: error.toString()}))
      .setMimeType(ContentService.MimeType.JSON);
  }
}

function sendConfirmationEmail(email, data) {
  var subject = "✓ ยืนยันการจองคิว - " + data.serviceName;
  var message = "สวัสดี " + data.name + "\n\n" +
                "ขอบคุณที่จองบริการของเรา\n\n" +
                "📋 รายละเอียดการจองคิว:\n" +
                "บริการ: " + data.serviceName + "\n" +
                "วันที่: " + data.date + "\n" +
                "เวลา: " + data.time + "\n" +
                "ระยะเวลา: " + data.duration + " นาที\n" +
                "ราคา: ฿" + data.price + "\n\n" +
                "หากมีคำถาม โปรดติดต่อเรา\n\n" +
                "ขอบคุณและรอคอยการมาของคุณ!";
  
  MailApp.sendEmail(email, subject, message);
}
```

### 3.3 เปลี่ยน Sheet ID
ในโค้ด หาบรรทัด:
```javascript
var sheet = SpreadsheetApp.openById('YOUR_SHEET_ID')
```

เปลี่ยน `'YOUR_SHEET_ID'` เป็น ID ที่คุณหาได้ ตัวอย่าง:
```javascript
var sheet = SpreadsheetApp.openById('1ABC23XYZ4567def')
```

### 3.4 Deploy (ส่งออก)
1. กดปุ่ม **"Deploy"** (สีน้ำเงิน บน)
2. เลือก **"New deployment"**
3. กดไอคอน ⚙️
4. เลือก **"Web app"**
5. ที่ "Execute as" เลือก **ชื่อ Google Account ของคุณ**
6. ที่ "Who has access" เลือก **"Anyone"**
7. กดปุ่ม **"Deploy"**

### 3.5 Copy Deployment URL
1. จะมี URL ปรากฏขึ้น เช่น:
   ```
   https://script.google.com/macros/s/1ABC23XYZ4567def/userweb?v=1
   ```
2. **Copy** URL นี้ (เก็บไว้)

---

## ⭐ STEP 4: เชื่อมเว็บกับ Google Sheets (ใช้เวลา 5 นาที)

### 4.1 แก้ไขไฟล์ HTML
1. ดาวน์โหลด `booking-website-advanced.html`
2. เปิดด้วย **Notepad++ หรือ Visual Studio Code**
3. หาบรรทัดนี้ (ประมาณบรรทัด 380):
   ```javascript
   let googleSheetURL = '';
   ```

4. วาง Deployment URL ที่ได้:
   ```javascript
   let googleSheetURL = 'https://script.google.com/macros/s/1ABC23XYZ4567def/userweb?v=1';
   ```

5. **Save** ไฟล์

### 4.2 Upload ไฟล์ใหม่ไปยัง Netlify
1. ไปที่ Netlify ที่เว็บไซต์เก่าของคุณ
2. ค้นหา **"Deploys"** tab
3. Drag and drop ไฟล์ HTML ที่แก้ไขใหม่
4. รอสักครู่
5. ✅ เสร็จแล้ว ตอนนี้จองคิวใหม่จะบันทึกใน Google Sheets

---

## 🧪 ทดสอบระบบ

### ขั้นตอน:
1. ไปที่ URL ของเว็บไซต์
2. **เพิ่มบริการใหม่** ตัวอย่าง:
   - ชื่อ: "ตัดผม"
   - รายละเอียด: "ตัดผมปกติ"
   - ราคา: "300"
   - ระยะเวลา: "30"

3. **จองคิวทดสอบ**:
   - ชื่อ: "ทดสอบ"
   - เบอร์: "0812345678"
   - อีเมล: "test@example.com"
   - บริการ: "ตัดผม"
   - วัน เวลา: (เลือกวันพรุ่งนี้)

4. **ตรวจสอบ**:
   - ✅ เว็บแสดง "จองคิวสำเร็จ" หรือไม่?
   - ✅ ไปที่ Google Sheets ขึ้นข้อมูลใหม่หรือไม่?
   - ✅ ได้รับ Email ยืนยันหรือไม่?

---

## 🎁 (อพชั่น) ขั้นตอนเพิ่มเติม

### A. ซื้อ Domain เป็นของตัวเอง (เช่น www.yourname.com)

#### วิธี 1: ราคาถูกที่สุด (NameSilo)
1. ไปที่ **https://namesilo.com**
2. ค้นหา Domain ที่ต้องการ (เช่น "mynails.com")
3. ราคาประมาณ $2-8/ปี
4. ซื้อและติดตาม Step ต่อไป

#### วิธี 2: เศรษฐศาสตร์กลาง (Namecheap)
1. ไปที่ **https://namecheap.com**
2. ค้นหา Domain
3. ราคา $5-10/ปี

#### วิธี 3: ของ Google
1. ไปที่ **https://domains.google.com**
2. ค้นหา Domain
3. ราคา $7-12/ปี

#### ตั้ง Custom Domain ที่ Netlify
1. ไปที่เว็บไซต์ Netlify ของคุณ
2. ไปที่ **Domain settings**
3. ทำตามคำแนะนำ
4. รอ 24-48 ชั่วโมง

### B. เก็บเงิน (Payment)

#### ตัวเลือก 1: QR Code PromptPay (ง่ายที่สุด)
```
1. ไปที่ https://promptpay.io
2. ใส่เบอร์โทรหรือ ID ประชาชน
3. ได้ QR Code
4. เพิ่ม QR ลงในเว็บหรือ Email
5. ลูกค้า Scan → ทำการ Transfer
6. ตรวจสอบจำนวนเงินใน Bank App
```

#### ตัวเลือก 2: Stripe (ระดับสากล)
```
1. ไปที่ stripe.com
2. สมัครสมาชิก (ต้อง KYC)
3. ได้ API Key
4. ปรับเว็บให้รับเงิน
(ต้องมี Developer ช่วย)
```

### C. Email Reminder อัตโนมัติ (ส่วนขั้นสูง)
```
Google Apps Script จะส่ง Email อัตโนมัติตอน:
- ลูกค้าจองคิว (ยืนยัน)
- 1 วันก่อนคิว (Reminder)
- 1 ชั่วโมงก่อนคิว (Final reminder)
```

---

## ❓ ปัญหาทั่วไป

| ปัญหา | วิธีแก้ |
|------|--------|
| **ข้อมูลไม่ขึ้นใน Google Sheets** | ✓ ตรวจสอบ Sheet ID ว่าถูกต้องหรือไม่ |
| | ✓ ตรวจสอบชื่อ Sheet (ต้องชื่อ "Sheet1" หรือ Sheet ที่ระบุใน Code) |
| | ✓ ไปที่ Google Apps Script กดปุ่ม Execute เพื่อทดสอบ |
| **ไม่ได้รับ Email** | ✓ ตรวจสอบ Gmail Spam Folder |
| | ✓ ตรวจสอบว่า Gmail ได้รับอนุญาตจาก Google Apps Script หรือไม่ |
| **เว็บช้า** | ✓ ใช้ Google Chrome เพราะ Firefox อาจช้า |
| **ลูกค้าไม่อยากจองเพราะเว็บเก่า** | ✓ ซื้อ Domain เป็นของตัวเอง |
| | ✓ ตั้ง SSL (HTTPS) - Netlify ให้ฟรี |

---

## 📞 ติดต่อหากมีปัญหา

### ช่องทาง Support:
- **Netlify**: https://support.netlify.com
- **Google**: https://support.google.com
- **ChatGPT**: ถามเกี่ยวกับ Code
- **Stack Overflow**: https://stackoverflow.com

---

## 🎯 สรุปขั้นตอน

```
1️⃣ Upload เว็บไปยัง Netlify ✅ (5 นาที)
   → ได้ URL: https://yoursite.netlify.app

2️⃣ สร้าง Google Sheets ✅ (5 นาที)
   → เก็บข้อมูลการจองคิว

3️⃣ สร้าง Google Apps Script ✅ (10 นาที)
   → บันทึกข้อมูลและส่ง Email

4️⃣ เชื่อม URL เข้ากับเว็บ ✅ (5 นาที)
   → ข้อมูลจะไปยัง Google Sheets โดยอัตโนมัติ

5️⃣ ทดสอบ ✅ (5 นาที)
   → ลองจองคิวดู ข้อมูลขึ้น Sheet หรือไม่

✨ เสร็จแล้ว ตอนนี้คุณมีเว็บจองคิวที่ใช้งานจริงแล้ว!
```

---

## 🚀 ขั้นตอนต่อไป (Advanced)

เมื่อคุณเก่งพอแล้ว:
- เพิ่มระบบจ่ายเงิน (Stripe/2C2P)
- เพิ่ม Reminder SMS
- เพิ่ม Calendar Sync (Google Calendar)
- ทำแอป Mobile
- เพิ่ม AI Chatbot

---

**💪 คุณทำได้! ถ้าติดตรงไหนบอกมา ผมช่วยได้**
